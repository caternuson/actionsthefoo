name: Foo Action

on: [push]

jobs:
  hello_world:
    runs-on: ubuntu-18.04
    steps:
      - name: Show hostname
        run: echo $HOSTNAME
      - name: Show workspace
        run: echo $GITHUB_WORKSPACE